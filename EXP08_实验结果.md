# EXP08 有效改动组合：准入审计结果

日期：2026-09-17。状态：**准入审计与补充复核已完成；组合训练和移除消融未执行，因为缺少两项符合计划条件的改动。保留 baselinev1。**

依据 `baselinev1_独立实验计划.md` 第 11 节：“只组合已经完成独立验证的改动”，并结合第 2 节的跨时间块/事件稳定性要求。本次核对 EXP01–EXP07 分支的最终报告，而非只读取初筛 `selection.json` 的胜出名称。选中候选不等于通过最终验证。

本次没有把 EXP03A、EXP04B 等已被后续验证否定的候选重新组合；没有根据已看过的 fold2 改选其他候选。即使暂时将 EXP01 列为保留候选，也只有一项，尚不足以开展多因素组合与逐项移除。这个结论不代表被淘汰方案之间绝无互补性；探索这种互补性需另列探索性实验，不能称为本计划的“已验证有效改动组合”。

## 各分支准入依据

| 实验 | 最终证据 | EXP08 处理 |
|---|---|---|
| EXP01 整体分母阈值 | 原 fold2 +0.8594 分；本次补充 fold0→fold1，与基线标签完全一致，无增益；后期收益集中于 P601B 单事件 | 暂留研究候选，稳定性证据不足 |
| EXP02 阈值迁移 | 02A 总分下降；02B 选中 α=0，等于基线 | 无新增有效改动 |
| EXP03 因果特征 | 历史选中的 03A 在 seed42/43/44 的 fold2 均下降；不能改选 fold2 上涨的 03D | 不准入 |
| EXP04 训练权重 | B c=2 在 seed43 的 fold2、seed44 的 fold1 退化；父实验明确不直接进入 EXP08 | 不准入 |
| EXP05 模型容量 | 最终 decision 的 adopted 为空；leaf50 后期与补充种子表现不稳定 | 不准入 |
| EXP06 正常模型与融合 | A 和选中的 B 在早期 fold1 均低于基线，后期诊断上涨不足以推翻该结论 | 不准入 |
| EXP07 报警后处理 | 两类选中候选都未通过父实验预先声明的长误报稳定性门槛 | 不准入 |

分支提交 ID、七份报告副本及哈希保存在运行目录的 `protocol.json` 和 `evidence/`。保留各父实验自己的判断标准；未把“所有种子必须上涨”等局部实验门槛追认为原计划对所有实验的统一硬性规定。

## EXP01 补充向前复核

唯一改变因素仍为设备阈值扫描的整体分母；复用同一历史折外概率，训练耗时为 0。直接加载记录提交中的 EXP01 `metrics.py`，不修改当前基线评分模块。

- fold1：仅使用 fold0 的有效监督概率校准，随后评估 fold1。
- fold2：使用 fold0+fold1 校准，随后复现已看过的 fold2 开发诊断。
- 设备至少两个有效校准异常事件才使用设备阈值，否则保留原共享回退。
- 所有块均来自既有历史，不能声称新获得了未见测试集。

| 配置 | fold1 分数 | 相对基线 | fold2 分数 | 相对基线 |
|---|---:|---:|---:|---:|
| baselinev1 | 70.486116 | 0 | 67.052841 | 0 |
| EXP01 | 70.486116 | 0 | 67.912243 | +0.859402 |

fold1 虽然 P106A/P601B 的阈值改变，但六设备预测标签均未改变。fold2 只有 P601B 改变：FP 从 1090 增至 1292，FN 从 54 降至 29，至少 18 点的长误报点数从 631 增至 787。整体准确率由 87.4251% 降至 86.2200%，加权召回由 46.6806% 升至 49.6044%。P202A 的 316 点异常仍然整段漏检。

这是阈值权衡的可复现局部收益，尚不能证明跨事件稳定提升。没有新增随机种子训练，也没有据此宣称组合有效。

## 本次实际完成的检查

- 核对六设备训练特征与标签文件 SHA256，并验证原始标签、事件完整的折边界和验证 CSV 对齐。
- 对比 baselinev1 与 EXP01 全部 18 对历史 CSV，时间戳、标签、原始概率一致。
- 重新计算两种阈值策略的 fold1/fold2，复现双方 fold2 原始预测标签。
- 保存每设备 FP/FN、FPR、普通及加权召回、阈值来源、每事件首次报警延迟及前 3/6 小时召回、全部误报段。未检出事件延迟为 null，无异常召回为 null。
- 使用当前 `predict.py` 重新推理六设备；校验三列格式、设备名、二元标签、测试时间戳与行数，以及与原基线预测标签一致。
- 核对原 `runs/baseline_v1/` 全部文件前后哈希未变。
- 当前基线 8 项单元测试通过，`git diff --check` 通过。补充标签一致性断言后进行了第二次独立输出目录复核。

初次审计共 4.70 秒，六设备推理 2.50 秒，主进程峰值 RSS 208.95 MiB；未包含子进程同步峰值。最终复核实测记录见 `resources.json`。沿用原始 402/536 个特征，未训练新模型。

## 复现与文件

```bash
.venv/bin/python experiments/exp08.py --output runs/exp08_new_audit
.venv/bin/python -m unittest discover -s tests -v
```

输出目录非空时拒绝覆盖。脚本依赖本地七个实验分支、原始数据、`runs/baseline_v1/` 与 `runs/exp01_global_threshold/`，不支持在缺少这些历史资产时凭空重建其他实验结论。其他分支准入意见来自本次人工核读的最终报告；若父实验更新，应重新审查意见。

- 最终复核目录：`runs/exp08_combination_recheck/`。
- 初次运行目录：`runs/exp08_combination/`。
- `decision.json`：准入判断、保留基线、未执行组合和移除消融的原因。
- `results.json`：两种策略的两折整体分数。
- `baseline/fold*/metrics.json`、`exp01/fold*/metrics.json`：设备、事件和误报段明细；同目录 CSV 保存历史概率与重新计算的标签。
- `baseline_predictions/`：本次基线六设备推理检查产物，不是新候选提交包。
- `verification.json`、`baseline_hashes.json`、`data_hashes.json`：校验依据。
- `evidence/`：七分支最终报告、EXP01 阈值实现、EXP08 运行脚本快照。

**没有生成新的候选提交包，没有上传 A 榜，没有修改训练或推理默认行为。** EXP08 的组合阶段仍需等待至少两项有充分独立验证依据的改动；当前按计划保留基线。
