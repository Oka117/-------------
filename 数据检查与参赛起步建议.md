# 数据检查与参赛起步建议

检查日期：2026-09-13。数据来源：当前目录 `data/` 的全部 18 个 CSV 和 `data.zip`；规则来源：[官方赛题页面](https://comp.kunlungpt.cn/player/competition/problem/2082685461163094018?eventId=2082273411774775297&index=1)的赛题详情、数据集说明、提交规范与评分规则。

## 结论

数据完整且可以直接开始探索。计算规模适合单机 CPU 起步，主要难点是异常事件少、设备之间差异大、时间分布变化，以及验证和阈值选择。建议先用分设备树模型建立完整基线，再判断是否需要时序神经网络。

以下工时是对熟悉 Python 和基本机器学习的一名参赛者的规划估计，不是训练实测或成绩承诺：首版可复现提交约 2–4 个完整工作日；较扎实方案累计约 2–3 周；持续冲榜累计约 4–8 周，成绩取决于未见异常的泛化。

## 全量数据检查

| 设备 | 特征数 | 训练行数 | 测试行数 | 异常点数 | 异常点占比 | 连续异常段数 |
|---|---:|---:|---:|---:|---:|---:|
| P106A | 536 | 20,016 | 8,903 | 1,024 | 5.12% | 5 |
| P202A | 402 | 20,016 | 8,899 | 341 | 1.70% | 2 |
| P310A | 402 | 20,016 | 4,798 | 410 | 2.05% | 2 |
| P310B | 402 | 20,016 | 2,278 | 3,907 | 19.52% | 7 |
| P412B | 402 | 20,016 | 10,850 | 940 | 4.70% | 1 |
| P601B | 536 | 20,016 | 2,278 | 4,519 | 22.58% | 10 |
| 合计 | 各设备不同 | 120,096 | 38,006 | 11,141 | 9.28% | 27 |

异常段按同一设备内连续 label=1 定义，不代表已确认的独立故障类型或独立物理事故。

- 压缩包约 215 MiB，CSV 合计约 1.2 GiB。ZIP 全部成员通过完整性校验；解压后的 18 个文件均与压缩包中的大小、CRC 一致。未取得官方校验和，不能据此确认是否为官方最新版本。
- 所有训练集均为 2024-01-01 00:00:00 至 2024-10-04 23:40:00，每 20 分钟一个点，覆盖 278 天。
- 所有测试集均从 2024-10-05 00:00:00 开始，与训练末尾相接。不同设备结束日期不同：P106A/P202A 为 2025-02-05，P310A 为 2024-12-10，P310B/P601B 为 2024-11-05，P412B 为 2025-03-04。
- 全部特征表均无时间戳缺失、重复、倒序或采样间隔异常；同设备训练与测试没有时间戳重叠。
- 标签只包含 0/1，行数、时间戳和顺序与训练特征全部一一对应。
- 全部特征表未检出 NaN、无法转换为数值的内容或正负无穷。这不排除用特殊数值编码缺失、传感器卡值等业务问题，匿名字段缺乏单位，不能仅凭负数认定错误。
- 同一设备 train/test 列结构一致。网页概述写 536 个特征，但实际 4 台设备只有 402 个特征，应动态读取，不应补齐后默认同名特征具有相同物理含义。
- 训练集没有全程恒定的特征；测试集 P310A 的 feature_99、P310B 的 feature_105 恒定。这可能是工况变化或卡值，尚未确认原因。
- 不计 timestamp，P106A 训练数据有 1 个重复特征行，P202A 测试数据有 2 个重复特征行。不同时间出现相同读数不应直接删除，尤其测试数据必须保留原始行数与顺序。
- 同设备训练/测试的整行特征哈希未发现重叠。

## 真正影响工作量的地方

### 1. 12 万训练点只有 27 段异常

同一异常中的连续点高度相关，不能视为数千个独立故障样本。P412B 的 940 个异常点全部来自 2024-07-11 至 2024-07-24 的同一段；P202A 和 P310A 各只有 2 段。

因此随机按行切分容易使同一异常同时出现在训练和验证中，分数偏乐观。大模型也不能自动弥补异常事件覆盖不足。

### 2. 简单留最后 20% 也不能解决验证

最后 20% 从 2024-08-10 09:20:00 开始：

| 设备 | 最后 20% 内异常点数 | 影响 |
|---|---:|---|
| P106A | 0 | 无法评估异常召回 |
| P202A | 316 | 主要检验一个事件 |
| P310A | 410 | 此前训练部分完全无异常 |
| P310B | 498 | 可以初步评估后续异常 |
| P412B | 0 | 无法评估异常召回 |
| P601B | 74 | 仅剩一个短事件，方差较大 |

建议主验证采用多个时间切点的向前验证，训练仅用验证之前的数据；按整段异常检查边界，避免切断同一个事件。根据最大特征窗口与数据相关性考虑隔离区间。标准工具可参考 [TimeSeriesSplit](https://scikit-learn.org/stable/modules/generated/sklearn.model_selection.TimeSeriesSplit.html)，但不能不检查标签就机械调用。

补充使用“整段异常及其前后正常时间块留出”做泛化诊断；若训练包含该事件之后的数据，应明确这是回顾性诊断，不是严格未来预测分数。P412B 只有一个异常段，无法同时满足独立事件监督训练和独立异常验证，需要比较正常数据建模的异常检测方法，并如实报告证据不足。

### 3. 训练与测试存在分布变化信号

按 |测试均值−训练均值| / 训练标准差 计算，超过 1 的特征数分别为 P106A 45、P202A 129、P310A 37、P310B 67、P412B 19、P601B 62。例如 P202A 的 feature_98 达到约 7.41。

这只是粗略分布变化诊断，不能区分工况、季节、故障占比和数据处理差异。不要因为测试分布不同便把它强制标准化到训练分布，也不能据此把测试点标为异常。用于模型拟合的缩放、筛选等统计量应先只从训练折计算。

## 评分决定优化方向

官网任务是逐时间点正常/异常二分类，不要求预测故障类型。计分优待异常区间起始阶段，尚不是“故障发生前若干小时预测”的定义，不能擅自把正常标签提前改为异常。

官方公式可写为：

\[
S=50\,\mathrm{Accuracy}+50\frac{\sum_{i:y_i=1}w_i\,\mathbf1(\hat y_i=1)}{\sum_{i:y_i=1}w_i}.
\]

对长度 L>1 的异常段，第 j 个点的权重为 `7−6(j−1)/(L−1)`；首点权重 7，末点权重 1。单点异常权重为 7。累计各设备的正确点数、总点数、异常命中权重和异常总权重时，异常段必须在设备内识别，不能跨设备拼接。

- 该指标不是 F1 或 AUC；二者可以辅助诊断，不能替代官方分数选择模型。
- 正常误报会损失准确率，漏报还会损失异常加权识别率，尤其是区间前端。应在验证集按官方指标调概率阈值，不默认 0.5。
- 差异化权重并未使每个事件贡献相同：L>1 的事件总权重为 4L，长事件仍有更大影响。报告总分之外，还应检查每个事件的起始漏检。
- 按全部训练标签演示：全预测正常的准确率为 90.72%，官方公式得分却仅约 45.36；全预测异常约 54.64 分。这是标签上的常量预测演示，不是已训练基线，也不是线上测试成绩。
- 无异常验证块的第二项分母为零，不能擅自当成满分。可合并多个有异常的验证块的分子分母，同时单独报告无异常块的误报率；仅含正常的结果不应与完整官方分数混称。

规则还说明 A 榜仅使用 3 台泵，B 榜使用全部 6 台；B 榜必须超过 80 分才具备晋级/评奖资格，该门槛不等于达到便能晋级。因此需要同时追踪 6 台设备表现，不能只针对 A 榜改进。

## 如何开始

1. **先实现评分与数据接口。** 动态读取各设备特征列，按 timestamp 检查标签对齐；保留原始数据。实现官方评分，检查全对、全错、全正常、全异常、单点异常和设备边界等案例。保存每次验证的概率与标签，便于独立调阈值。
2. **优先在 P601B 与 P310B 建第一版。** 它们分别有 10 和 7 段异常，更适合先检查训练、验证和误报分析流程。随后扩展全部设备，尤其单独处理 P412B 的事件稀缺问题。
3. **建立分设备 LightGBM 基线。** 先只用当前时刻原始特征，限制树复杂度，并按时间验证。LightGBM 是树提升框架，支持高效 CPU 训练，见[官方文档](https://lightgbm.readthedocs.io/en/stable/)。选择它是起步方案建议，尚无本数据上的效果实测。不要默认相同 feature 编号跨设备语义相同，也不要把绝对日期作为第一版主特征。
4. **再添加少量因果时序特征。** 在筛选的特征上加入 1 小时、6 小时、24 小时的历史均值、标准差、变化量，对应 3、18、72 个采样点。只使用预测时可获得的当前和历史数据，不使用居中窗口、未来回填或未来标签。先控制特征数量，避免全部字段乘所有窗口。
5. **增加正常数据建模的对照。** 在训练折正常样本上拟合稳健标准化加 PCA 重构误差或 Isolation Forest，重点看未见事件召回及正常工况误报。异常检测同样可能把新工况误报为故障，需要时间验证，不应默认优于监督模型。
6. **最后做阈值与报警策略。** 比较统一阈值、适度分设备阈值和轻量连续性处理。连续多个点才报警可能减少误报，却延迟起始识别，应按官方分数权衡。避免依赖验证集真实标签反向填满整段报警。
7. **完成可复现提交。** 六个 `泵号_predict.csv` 打包为 result.zip，同时提交 submission_code.zip；固定随机种子，保存依赖、模型与特征列表，支持命令行传入数据目录和输出路径。

## 工作量与算力预算

| 阶段 | 预计有效工时 | 完成标准 |
|---|---:|---|
| 规则确认、评分器、数据接口和验证设计 | 0.5–1 天 | 能解释每台泵的验证集包含哪些异常 |
| 分设备基础模型和提交生成 | 1–2 天 | 一次命令生成全部结果，完成本地复现 |
| 首版错误检查与一次迭代 | 0.5–1 天 | 逐事件检查漏报和正常误报 |
| 时序特征、正常模型、多折实验 | 额外 5–10 天 | 有消融比较，改进不只来自一个事件 |
| 冲榜、鲁棒性、材料与复现 | 额外 2–5 周 | 6 台设备兼顾，提交包可独立运行 |

以上为一人熟悉 Python/机器学习、每天约 6–8 小时的估算，各阶段有重叠。每周投入 10–15 小时，首版可安排约 1–2 周，较完整方案约 5–8 周。是否超过 80 分必须实验后判断。

全量原始数值特征以 float32 紧凑存放理论上仅约 268.6 MiB（不含 DataFrame、模型和中间副本）。本次 ZIP 校验和全量质量扫描脚本耗时约 12 秒，不包含网页访问与环境准备。数据规模本身不是算力瓶颈；逐设备运行，16 GB 内存级别机器适合作为初步预算，CPU 即可起步。这里没有成功读取本机硬件配置，也尚未实测模型训练时间或峰值内存。先用一台泵计时，再估算 6 台 × 验证折数 × 参数组合；现阶段没有购买 GPU 的依据。

## 提交前尚需核实的细节

- 页面写结果包含“泵号、timestamp、label”三列，但未给泵号列的明确英文名称，本地压缩包也没有提交样例，应向平台样例或答疑确认，不能自行假设为 pump_id。
- 页面一处描述模型加载与推理复现，另一处要求完整训练+推理。建议同时提供训练与预测命令及模型文件，并确认最终复现口径。
- 页面 README 示例使用 result.json，而结果规范要求 CSV 打包 ZIP。应遵循明确的结果规范，并在开发提交器前确认路径参数含义。
- 本次未见可下载的官方评分脚本；跨设备汇总先按页面公式的总分子/总分母实现，并保留逐设备分数，拿到官方脚本后核对。仅正常数据的异常项处理也需核对。
- 页面显示每队每天最多 5 次提交，团队规模 1–3 人；报名截至 2026-10-30，比赛时间至 2026-11-20。最终提交节点需继续核对赛事整体日程，不能将总赛程末日直接当作初赛提交截止时间。

本次只检查数据并提出方案，未训练模型、报名或上传任何比赛数据。
