# 下一轮实验计划：EXP09–EXP11

日期：2026-09-17。状态：待执行。本文件只制定实验，不表示已训练或取得收益。

建议顺序：**EXP09 冻结验证协议 → EXP10 低成本探索性组合 → EXP11 正常状态评分补充**。每步可以独立停止，不要求为完成编号而训练全部方案。继续保留 `runs/baseline_v1/`。

## 1. 为什么这样安排

EXP08 审计没有发现两项已通过独立验证、可直接组合的改动。下一轮不重复大网格，而是回答两个具体问题：

1. EXP01 提高召回但增加误报，EXP07 的 2 点平滑降低部分误报但损失召回，两者是否互补？这是尚未验证的组合假设，可以进行探索性四组对照，不需要先宣布单项有效。
2. EXP06 只用 PCA 重构误差，是否遗漏了保留主成分方向上的异常变化？加入该方向的距离作为独立候选，检查能否改善少故障设备，同时控制正常工况误报。

现有事实：EXP01 在 fold1 与基线标签一致，fold2 +0.8594 分；EXP07 的 2 点平滑在 fold2 +1.0638 分，但加权召回下降且未过父实验长误报门槛。两项分数不能直接相加。EXP06 的后期上涨不能抵消其早期明显下降。

本轮标签复查：P106A/P202A/P310A/P310B/P412B/P601B 分别有 5/2/2/7/1/10 段连续异常。这些是标签段，不是经过确认的独立故障类型。

| 设备 | 关键证据限制 | 可以回答的问题 |
|---|---|---|
| P202A | 6 月 25 点异常、9 月 316 点异常；只有一次后续异常可用于迁移诊断 | 新方案能否检出后一次异常，代价是多少；不能据此证明稳定的新故障泛化 |
| P310A | 8 月、9 月各一段；前期监督训练为单类 | 区分首次故障冷启动与已有异常后的迁移表现 |
| P412B | 只有 7 月一段异常 | 正常模型首次故障诊断；无法建立监督训练后再检出另一独立故障的证据 |
| P106A/P310B/P601B | 有更多标签事件，仍可能属于相同故障或工况 | 作为跨事件、跨时间稳定性检查的主要来源 |

## 2. EXP09：事件完整的滚动验证与可复用缓存

**目标：补足评估基础，不寻找新模型。** 输出每个设备实际具有多少个合法“历史训练→过去校准→未来评估”的机会。

### 2.1 固定时间协议

沿用原三块结果作为兼容性检查。另在训练数据内建立月度时间块，候选边界为 2024-03-01、04-01、05-01、06-01、07-01、08-01、09-01 和训练结束。

- 每设备将切开异常的月界统一前移到该事件开始；重复边界合并。先固定完整时间块分区，再生成滚动任务。
- 以紧邻评估块的前一完整块作为校准块；模型仅使用校准块之前的历史训练，并留至少 72 点隔离、保护事件边界。
- **同一个模型**先预测校准块、再预测评估块；不在两块之间重训。用于检查阈值迁移时，避免同时混入模型重训导致的尺度变化。
- 对每个评估月份汇总六设备对应块。若某设备边界合并导致缺少该块，显式报告缺失和参与设备，不伪造六设备完整分数；候选与基线必须使用完全相同的行。
- 不满足训练或校准长度的任务标为不可用，不临时缩短隔离期。训练只有单类时维持基线常数行为并单列。
- 各任务只能读取本任务评估开始之前的校准标签；全设备共享阈值池须额外截断到所有目标设备中最早的评估开始，且不能切开校准异常。若池无有效异常，使用原 0.5 回退并标注。
- 3–6 月作早期开发诊断，7–8 月作固定候选稳定性复核，9 月以后仅作已查看的后期诊断。边界移动后，按任务名义月份归组，并披露实际起止时间。

上述数据过去已经被研究过，因此不是新的独立测试；用途是排查单事件依赖和阈值迁移，不能消除历史选择偏差。

### 2.2 输出与验收

保存 `split_manifest.json`、`event_inventory.csv`、每任务模型与概率、校准资格、训练/校准/评估实际边界、数据和代码哈希。任何训练或校准标签进入对应评估段均为失败。

两类指标必须分开：

- 点级：本地整体分数、Accuracy、加权召回、每设备 FP/FN/FPR。
- 事件级：每事件首次命中延迟、前 3/6 小时召回、事件加权召回、整段漏检；正常误报的长段点数与持续时间。

同一事件在不同任务重复出现时，跨任务总表只计一次；若无法去重，分别报告任务结果，不把相关重复任务当成独立样本。跨月份边界前移后的事件权重在完整事件上计算。

先只跑 seed42 基线，并计时两个任务；据实估计其余 CPU 工作量。最多约六个月度评估起点 × 六设备，实际数量由合法分区决定，不承诺固定耗时。仅对后续入围的训练方案补 seed43/44。

输出目录：`runs/exp09_forward_protocol/`。拟新增 `experiments/exp09.py` 与共享诊断模块。此处路径是计划，不表示入口已经实现。

## 3. EXP10：阈值策略 × 2 点因果平滑的探索性组合

**优先执行的模型方案实验。** 父实验：EXP01 与 EXP07B。不将其写作原 EXP08 的已验证组件组合。

固定原模型、原始特征、训练权重、seed42、设备阈值资格和回退策略，只做四组：

| 组别 | 阈值目标 | 概率处理 | 用途 |
|---|---|---|---|
| C0 | 原设备目标 | 无 | 基线 |
| C1 | EXP01 整体分母 | 无 | 仅阈值 |
| C2 | 原设备目标 | 当前及前一点均值 | 仅平滑 |
| C3 | EXP01 整体分母 | 当前及前一点均值 | 组合 |

平滑使用 `(p[t]+p[t-1])/2`，开头只有一个点时使用该点；校准和评估各自冷启动，与 EXP07 约定一致。每组都在本组校准概率上重新精确搜索阈值；不能给平滑概率套用未平滑阈值。整体分母只来自同一合法校准池，不包含评估标签。

### 执行顺序

1. 先用原 baselinev1 缓存复现 C0/C1/C2，验证与原分支对应结果相同；再运行 C3。此阶段训练 0 次。
2. 用 EXP09 缓存运行固定四组，核对时间和设备一致性，不按看到的结果增加窗口或改成设备专属组合。
3. 若 C3 值得保留，再使用合法缓存或重新训练的 seed43/44 **配对基线概率**复核，四组均使用同种子模型。不需要为阈值和平滑分别重训。
4. C3 去掉阈值因素即 C2，去掉平滑即 C1；四组已经构成逐项移除消融，无需另开无控制的混合方案。

报告交互量 `I = S(C3) - S(C1) - S(C2) + S(C0)`；I 为正仅说明在该评分和样本上有交互，不等于泛化有效。C3 若不优于较好的单项，即使超过基线，也优先保留较简单单项。

**停止规则：**若固定 C3 在早期开发块的配对平均分不高于 C0，或不高于较好的单项，则停止组合路线，不扩展窗口/比例网格。仍保存完整诊断，不能只报成功块。

输出：`runs/exp10_threshold_smoothing/`。拟新增 `experiments/exp10.py`，共享 EXP01 扫描与 EXP07 因果平滑实现；避免直接合并整条实验分支覆盖当前文件。

## 4. EXP11：PCA 保留空间距离能否补充重构误差

**独立的新评分假设，第二优先级。** EXP06 不通过早期验证，不代表每种正常状态评分都无效；本实验也不能预设 P202A 会改善。

沿用 EXP06 的训练正常样本、稳健缩放、95% 方差 PCA。主成分数量和全部统计量只在训练正常样本拟合。

- Q：原 PCA 重构均方误差，复现 EXP06A，作为正常模型对照。
- T²：保留空间的标准化平方距离，`sum(z_j² / max(lambda_j, epsilon))`，其中 z 为中心化缩放后样本的投影，lambda 为训练 PCA 特征值。
- 保护项固定为 `epsilon = max(1e-12, 1e-6 * 最大特征值)`；不按评估结果改值。

第一阶段只比较 **监督基线、Q、T²**，不增加新的融合权重网格。PCA 可共用一次拟合，保存两类分数。

使用同一合法历史校准协议为 Q、T² 各自选阈值。为避免直接跨设备比较原始距离量纲，使用训练正常分数的右连续经验 CDF 映射；Q 与 T² 都使用相同映射规则。这是新协议，必须和原 EXP06A 单独标明，不能把阈值映射变化全部归因于 T²。

若要归因到评分本身，必须以“Q + 同一 CDF/阈值协议”为直接对照。没有有效校准异常的设备保留预先固定的正常分位数 0.995，只解释为正常误报约束的候选；与基线同时报告，不声称已校准故障召回。经验 CDF 样本少、尾部离散及训练内拟合乐观性须报告。

先用 EXP09 全部固定任务比较 Q/T²；重点看 P202A 的第二次事件、P310A 的两种训练状态、P412B 的首次事件，但**不据这些已知事件定设备专属阈值**。选择使用早期开发块，多事件设备的误报与召回必须同时报告。

**停止规则：**若 T² 相对同协议 Q 没有改善早期平均分，或收益仅来自一个后期事件且其他块明显退化，停止；不再增加 Isolation Forest、autoencoder 等模型大网格。若 T² 胜过 Q 但未胜监督基线，可保留研究记录，不能直接替换基线。只有出现稳定增量信号后，才另列监督分数融合实验。

输出：`runs/exp11_pca_retained_distance/`。拟新增独立评分模块与 `experiments/exp11.py`；保留训练统计量、投影矩阵、特征值、映射样本和版本。

## 5. 本轮统一判断规则

以下是**新实验的预声明工程门槛**，不是统计显著性结论，也不追溯修改 EXP01–EXP07 的结论。运行前写入各自 `protocol.json`。

- 固定候选先在早期开发任务筛选；7–8 月复核及后期诊断用于保留/否决，不能失败后在同一批结果上改选另一候选。
- 优先要求固定候选相对同协议基线的任务平均分提高至少 0.25 分；总体汇总分另报。没有异常的任务不计算异常总分，只贡献正常误报诊断。
- 在具备异常的非重复评估任务中，至少 2/3 的配对分数变化非负，最差任务下降不超过 1 分。少于三个合法有异常任务时标记证据不足，不强行计算稳定通过。
- 相比基线，不新增整段漏检；出现局部延迟、早段召回、长误报退化时完整报告。跨任务去重后的总长误报点数最多增加 10%；基线为 0 时须保持 0。这些容忍度只是本轮冻结的实用筛选标准。
- 对训练相关入围候选，以 seed42/43/44 的配对结果确认：至少两个种子的跨任务平均增益为正，三种子总平均达到上述 0.25 分，单种子平均下降不得超过 0.5 分。确定性 PCA 评分不通过无意义地重复相同拟合制造“多种子证据”。
- 如果整体收益超过一半来自同一事件或同一设备，标记“收益集中”，不称为广泛稳定提升；附去掉该事件/设备后的配对得分变化，保持完整事件权重。

通过这些规则只表示“值得保留的本地候选”，不是“证明线上提分”。规则过严导致无候选时如实保留基线，不能事后改门槛宣布成功。

## 6. 资源、交付和返回 EXP08 的条件

- 全程先用现有 CPU；EXP10 优先复用概率缓存，EXP11 的 Q/T² 共用 PCA 拟合。不申请 GPU。
- 不覆盖既有 runs；每个实验保存配置、分支提交、源码差异、数据哈希、概率、逐设备/事件明细、资源和决策。
- 训练实现与推理使用同一特征/变换逻辑。入围候选再做六设备行数、时间戳、特征顺序、137 点分块一致性、模型和 manifest、打包源码及依赖检查。
- 只有实际通过后才生成独立候选提交包；本计划不包含上传或自动试榜。
- 若 EXP10 的组合有效，可作为探索性组合结果保留，不必重写父实验为“单项有效”。只有确有至少两项独立有效改动时，才恢复原 EXP08 的确认性组合路径。

**第一批实际工作范围：只执行 EXP09 和 EXP10。** 两者完成并总结后，再决定是否执行 EXP11。这样能先回答最便宜且具体的互补性问题，避免再次先训练大量模型再寻找解释。

依据：`baselinev1_独立实验计划.md`、`EXP08_实验结果.md`、`runs/exp08_combination_recheck/evidence/` 中各分支最终报告，以及本轮只读复查的六设备训练标签。所有机制解释和新方案收益均为待验证假设。
