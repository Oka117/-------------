# EXP09 实验结果

日期：2026-09-17。**状态：已完成；滚动协议与可复用缓存验收通过，保留 baselinev1。**

本实验只建设验证基础，不比较新模型，不产生提分候选。按《下一轮实验计划_EXP09–EXP11.md》第 2 节执行，继承原独立实验计划的数据、诊断和留痕约束。本次没有执行 EXP10/EXP11，也没有生成或上传提交包。

## 1. 执行范围与冻结选择

- 对照：`runs/baseline_v1/`；代码起点：`baseline_v1_exp09` / `c5fd7b26b12a798a587aab674b0d94454522297c`。
- 唯一变化：验证协议；原始特征、LightGBM 参数、300 轮、seed42、4 线程及原阈值扫描算法不变。
- 新代码：`experiments/exp09.py`、`experiments/diagnostics.py`、`experiments/verify_exp09.py`；测试：`tests/test_exp09.py`。
- 输出：`runs/exp09_forward_protocol/`。非空目录拒绝覆盖；原 baselinev1 全目录哈希前后一致。
- 月界为 3 月 1 日至 9 月 1 日及训练结束；切开异常时前移至事件开始，重复边界合并。3 月是首个完整校准块，因此评估名义月份是 **4–9 月**，共 6 个起点。9 月块延续至 10 月 4 日 23:40。
- 每任务以紧邻评估的上一完整块校准，在校准块前训练并保留至少 72 点隔离；校准和评估使用同一个模型。
- 计划未给出最短长度数值，运行前冻结为训练至少 100 点、校准至少 72 点。这是可用性工程约束，不是经调参得到的门槛；实际 36 个任务全部满足。单类训练使用常数模型。
- 设备独立阈值要求混合类别训练模型、校准至少 2 段异常及正常样本；否则使用共享池。共享池只纳入合格模型的校准概率，额外截到本月所有设备中最早的评估开始，遇到被截异常则去掉整段；无有效异常时回退 0.5。
- 4–6 月早期开发、7–8 月稳定性复核、9 月后期诊断；**这些数据此前已查看，不是新的独立测试**。完整标签仅用于事件隔离分区和事后指标，拟合与阈值选择遵守任务时间边界。

## 2. 合法任务和异常覆盖

共 **36 个合法任务、0 个缺失任务**；20 个混合类别 LightGBM、16 个单类常数模型。共 16 个有异常的设备任务、20 个无异常设备任务。无异常任务的异常召回及异常总分为空，只报告准确率与误报；月度整体分数则合并参与设备的点级统计。

| 设备 | 合法任务 | 混合类别模型 | 有异常评估任务 | 评估异常段 / 全部标签段 | 整段漏检 | FP | FN |
|---|---:|---:|---:|---:|---:|---:|---:|
| P106A | 6 | 5 | 3 | 4 / 5 | 1 | 1004 | 437 |
| P202A | 6 | 2 | 2 | 2 / 2 | 2 | 0 | 341 |
| P310A | 6 | 0 | 2 | 2 / 2 | 2 | 0 | 410 |
| P310B | 6 | 6 | 3 | 4 / 7 | 2 | 6106 | 951 |
| P412B | 6 | 1 | 1 | 1 / 1 | 1 | 0 | 940 |
| P601B | 6 | 6 | 5 | 6 / 10 | 0 | 9657 | 511 |

去重后共 **81,047 行、19 段完整评估异常、8 段整段漏检**；同一设备时间戳与事件 ID 无重复。其余 8 段历史异常位于评估范围之前，不作为额外独立评估事件。连续标签段不等于经过确认的故障类型。

实际边界移动：

| 设备 / 名义边界 | 前移点数 | 实际开始 |
|---|---:|---|
| P106A / 4 月 1 日 | 263 | 3 月 28 日 08:20 |
| P310B / 8 月 1 日 | 38 | 7 月 31 日 11:20 |
| P601B / 8 月 1 日 | 771 | 7 月 21 日 07:00 |

因此 4 月共享池截止 3 月 28 日 08:20，8 月截止 7 月 21 日 07:00，均严格不包含该时间点及以后的标签。8 月合法共享池不含异常，六设备均回退 0.5。详见 `calibration_pool_audit.csv`；共 4 个任务使用设备阈值、26 个使用共享阈值、6 个回退。

## 3. 月度基线结果

以下全为本次新协议基线，不应与旧 fold2 的 67.052841 相减并解释成模型退化或提升：两者训练截止、校准范围和模型是否重训均不同。评分仍是本地复现公式，未重新核定官方跨设备汇总口径。

| 名义评估月 | 阶段 | 参与设备 | 行数 | 整体分数 | Accuracy | 加权召回 |
|---|---|---:|---:|---:|---:|---:|
| 2024-04 | 早期开发 | 6 | 13,223 | 67.463099 | 69.5152% | 65.4110% |
| 2024-05 | 早期开发 | 6 | 13,392 | 65.397923 | 71.1470% | 59.6489% |
| 2024-06 | 早期开发 | 6 | 12,960 | 83.201383 | 70.5401% | 95.8626% |
| 2024-07 | 稳定性复核 | 6 | 12,583 | 37.940078 | 75.8802% | 0.0000% |
| 2024-08 | 稳定性复核 | 6 | 14,201 | 58.771157 | 82.4097% | 35.1326% |
| 2024-09 | 已查看后期诊断 | 6 | 14,688 | 61.066488 | 78.8194% | 43.3135% |

| 阶段 | 月度分数算术平均 | 合并点级分数 | 有异常设备任务分数平均 |
|---|---:|---:|---:|
| 4–6 月 | 72.020802 | 71.625405 | 58.771829 |
| 7–8 月 | 48.355617 | 51.487965 | 39.910461 |
| 9 月以后 | 61.066488 | 61.066488 | 58.998386 |

所有不重复评估行合并：分数 **63.025728**、Accuracy **74.8825%**、加权召回 **51.1690%**。月度平均、设备任务平均和合并点级分数是不同统计量，均单独保存，不相互替代。

## 4. 关键诊断与证据限制

- **P310A：6 个任务全部为常数 0 模型。** 首段异常在 8 月，9 月任务必须在 8 月校准块之前训练，因此仍未见异常。两段评估异常均漏检。这暴露了固定旧模型协议下的冷启动限制；本 EXP09 缓存本身不能回答“见过 8 月异常后，重训模型对 9 月事件能否迁移”。不得把这项未覆盖问题宣称已验证。
- **P202A：两段异常均漏检。** 8–9 月任务已经具备混合类别训练历史，但只有 9 月一段后续异常能检查迁移，稳定性证据有限。
- **P412B：唯一异常发生在冷启动评估中，整段漏检。** 9 月任务虽已有混合类别训练历史，却没有后续异常可检验。
- **7 月整体加权召回为 0。** 该名义月的完整异常仅来自 P412B；跨月的 P601B 事件完整归入 8 月块，因此不能按自然月重新拆分标签后解释此数。
- **误报集中于 P601B/P310B。** 两者分别 9,657 / 6,106 个 FP；全设备 16,767 个 FP。跨月相邻误报段合并后有 152 段，持续至少 18 点（6 小时）的误报共 **16,173 点**。
- 去重误报段持续时间：中位数 260 分钟、P90 3840 分钟、最长 49720 分钟。持续时间按采样格数量 ×20 分钟计；各任务段和跨任务合并段分别保存。
- 每事件首次命中延迟、前 3/6 小时召回、加权召回及 missed_event 已保存；整段漏检延迟为空，短事件只使用真实长度。事件权重在完整事件上计算，跨月不重置。

## 5. 资源与验收

- 前两个任务训练耗时分别 0.0003 / 1.15 秒，含一个常数任务，外推余下 34 个任务约 20.1 秒；实际混合模型更慢，初估偏低，原始估计保留于 `pilot_timing.json`。
- 实际训练总计 **56.02 秒**；预测与缓存写盘 **0.68 秒**；完整实验 **63.51 秒**；峰值 RSS **723.39 MiB**。各任务单独计时，特征名单保存在各任务 manifest。
- 12 项单元测试全部通过，覆盖原基线、跨月异常、重复边界、共享池截断、漏检延迟和短事件窗口。
- 旧三块缓存 18 份的时间戳、标签、完整事件权重、阈值及预测复现通过；旧 fold2 分数 67.052841。此兼容检查复用已有概率，没有重新训练旧三折。
- 36 个任务模型/常数状态全部重载；校准与评估所有行采用 137 点分块重推，概率与缓存一致（绝对容差 1e-14）。
- 单独重载验收耗时 7.36 秒，未计入上述训练流程耗时。
- 训练/校准/评估不重叠、72 点隔离、完整事件、共享池时间边界、跨任务去重、数据与基线哈希均通过。源码快照、源码哈希、Git 起点/差异、数据哈希和缓存哈希已归档。

## 6. 复现及缓存接口

```bash
.venv/bin/python -m unittest discover -s tests -v
.venv/bin/python -u experiments/exp09.py --output runs/exp09_forward_protocol_new
.venv/bin/python experiments/verify_exp09.py --run runs/exp09_forward_protocol_new
```

本次实际训练输出为默认 `runs/exp09_forward_protocol/`。复现时必须选新目录；验证器也拒绝覆盖已有验收结果。

- `protocol.json`：训练前冻结的协议及未来候选门槛。
- `split_manifest.json` / `task_inventory.csv`：名义月份、实际边界、训练/校准/评估事件数、可用性和模型资格。
- `event_inventory.csv`：所有 27 段历史标签异常。
- `tasks/<设备>_<月份>/manifest.json`、`model.txt`：任务状态、特征顺序、常数或模型、耗时。常数模型无需 txt。
- `cal_probabilities.csv` / `eval_probabilities.csv`：原始 row_index、timestamp、label、probability、完整 event_weight；供后续固定对照复用。共享池仍须遵守其 cutoff，不能直接拼接未经截断的校准文件。
- `evaluation.csv` / `metrics.json`：最终基线预测、阈值来源和任务诊断。
- `task_metrics.csv` / `event_metrics.csv` / `false_alarm_segments.csv` / `deduplicated_false_alarm_segments.csv`：设备、事件、误报段明细。
- `calibration_pool_audit.csv`：每月每设备共享池资格、保留/排除行数和截止时间。
- `results.json`、`resources.json`、`compatibility.json`、`verification.json`、`reload_verification.json`：汇总、资源及验收。

**结论：EXP09 基础设施完成，可为 EXP10 提供配对概率缓存；本轮没有新模型收益结论，保持 baselinev1。**
