# EXP10 实验结果：整体分母阈值 × 2 点因果平滑

日期：2026-09-17。状态：**已执行，停止组合路线，保留 baselinev1**。

依据《下一轮实验计划_EXP09-EXP11.md》第 3、5 节。父实验为 EXP01、EXP07B，使用 EXP09 seed42 缓存；未重新训练、未执行 EXP11、未生成提交包。

## 配置与协议边界

固定原模型、特征、训练权重和 seed42；C0 为原设备阈值目标，C1 仅改整体分母，C2 仅加 2 点因果平滑，C3 同时采用两者。每组重新精确搜索阈值。平滑在各校准块和评估块分别冷启动，首点使用自身，后续使用当前及前一点均值。

**本次有一项明确的协议修订，不能把月度 C0 直接称为原 EXP09 完全复现。** 原 EXP09 的设备本地校准可以晚于共享池的最早评估截止时间；P601B 的 4 月任务触发了这一问题。若直接套用共享分母，本地搜索样本未完全包含于分母池。第一次运行被断言中止，未进入月度结果选择。

修订后，四组的本地阈值搜索统一使用共享截止时间之前的完整事件前缀；原模型资格、至少两段校准事件的资格判断及共享池/0.5 回退规则保留。资格按原完整本地块判断，实际搜索仅用合法前缀；前缀无有效类别时沿用搜索器的 0.5 回退。修订写入 protocol.json 后运行四组。这样比较的改变因素一致，但属于对计划未明确冲突情况的实施修订。

与原 EXP09 比较，C0 仅 P601B_2024-04 的 36 个预测改变，该任务分数 +0.833333；另外 35 个任务预测相同。详见 `C0_vs_exp09.csv`。原 baselinev1 与 EXP09 文件均未改动。

## 原缓存复现与组合

C0/C1/C2 共 36 组设备预测与父实验逐行一致。原 fold1 用 fold0 校准；fold2 用 fold0+fold1 校准。以下为跨设备汇总分。

| 组别 | fold1 | fold2（已查看的开发诊断） |
|---|---:|---:|
| C0 基线 | 70.486116 | 67.052841 |
| C1 仅整体分母 | 70.486116 | 67.912243 |
| C2 仅平滑 | 70.681479 | 68.116616 |
| C3 组合 | 70.666545 | 67.610224 |

C3 两个块均低于 C2。交互量 I = C3 − C1 − C2 + C0：fold1 为 −0.014934，fold2 为 −1.365795。单项增益没有叠加。

## 月度滚动对照

36 个设备任务、81,047 个互不重复的评估点、19 个完整异常事件。以有异常的设备月度任务为主要平均单位；无异常任务只贡献误报及整体汇总诊断。名义月份分组，披露实际时间边界于 EXP09 split_manifest.json。3 月仅作校准，不存在合法 3 月评估任务。

| 阶段 | 有异常任务数 | C0 | C1 | C2 | C3 | C3−C0 |
|---|---:|---:|---:|---:|---:|---:|
| 早期 4–6 月 | 8 | 58.875996 | 58.777616 | 59.116449 | 59.116449 | +0.240453 |
| 稳定性 7–8 月 | 4 | 39.910461 | 39.910461 | 39.912208 | 39.912208 | +0.001747 |
| 后期 9 月任务 | 4 | 58.998386 | 58.998386 | 59.055784 | 59.055784 | +0.057398 |

早期交互量均值 +0.098380，但 C3 与 C2 持平，不能说明组合有额外价值。早期按三个“月份跨设备汇总分”再平均，C3−C0 为 +0.187970，同样不超过较好单项且不足 0.25 分。该结论不依赖这两种平均口径的选择。

## 门槛、事件与误报

- **停止条件触发：C3 早期平均分不高于较好单项 C2。** 不扩大窗口或比例搜索，不继续 seed43/44。
- 早期平均增益 +0.240453，未达 +0.25 门槛。8 个任务中 6 个不下降（75%），最差下降 0.740741 分；后两项通过，但不能抵消停止条件。
- 新增整段漏检 0；前 3/6 小时召回下降的事件均为 0；1 个事件首次命中延迟变差，细节见 paired_events.csv。
- 跨任务连接连续误报段后，长误报点数：C0 16,096，C1 16,155，C2/C3 16,252。组合增加 156 点（约 0.97%），未超过 10% 门槛。
- 收益集中：P601B 2024-05-22 事件贡献了早期汇总净收益的超过一半。全期收益也集中于 P601B；不称为广泛稳定提升。按固定总分分母分解贡献，并保存逐设备/逐事件移除后的配对结果；移除事件保留其他完整事件原权重。

逐任务 FP/FN/FPR、Accuracy、加权召回、阈值来源、事件延迟和前段召回、误报段起止/点数/时长均已保存。后期诊断只用于完整披露，不用于失败后重新选方案。

## 交付与复现

有效完整结果目录：`runs/exp10_threshold_smoothing/completed/`。父目录中的第一次尝试保留为中止记录，不是最终结果。

```bash
.venv/bin/python experiments/exp10.py --output runs/exp10_threshold_smoothing/completed
.venv/bin/python experiments/exp10_diagnostics.py runs/exp10_threshold_smoothing/completed
.venv/bin/python -m unittest discover -s tests -v
```

重复运行需指定新的空输出目录。核心扫描和平滑实现分别快照自 EXP01、EXP07 分支，共用诊断模块来自 EXP09；来源提交、源代码快照和哈希随结果保存。未合并父实验整条分支。

主要文件：

- `protocol.json`、`decision.json`、`results.json`：冻结规则、决策和分组结果。
- `historical_scores.csv`、`monthly_scores.csv`、`paired_tasks.csv`：父实验复现、月度对照及交互量。
- `task_metrics.csv`、`event_metrics.csv`、`paired_events.csv`：逐任务及事件指标。
- `deduplicated_false_alarm_segments.csv`、`gain_concentration.csv`、`leave_one_out.csv`：长误报、收益归因和移除诊断。
- `calibration_pool_audit.csv`、`C0_vs_exp09.csv`：合法校准池和协议修订影响。
- `verification.json`、`input_hashes.json`、`source_hashes.json`、`resources.json`：校验、来源及资源。

主实验耗时约 3.83 秒，峰值 RSS 181.1 MiB，训练 0 次。11 项测试全部通过，包括共享分母精确扫描与枚举对照、概率并列、因果性、137 点分块一致性、冷启动及事件边界。概率缓存/数据哈希已核验，基线和 EXP09 文件哈希保持一致。

本地分数为现有公式实现；这些历史数据已经查看过，不是新的独立测试。线上 A 榜未提交。本次没有可替换 baselinev1 的组合候选。
