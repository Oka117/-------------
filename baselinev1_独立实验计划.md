# baselinev1 独立实验计划

日期：2026-09-13。

目标：以 baselinev1 为固定对照，通过独立消融确认哪些改动能改善异常识别、起始响应和正常误报，再组合有效改动。

本文是待执行实验计划。除基线诊断外，所有实验收益均未验证，不预估线上提分。本文不修改训练、推理代码，也不表示下述配置参数已经实现。

## 1. 基线与问题定位

### 1.1 固定对照

- 线上 A 榜：**81.9323**，来自用户反馈。
- 本地运行：`runs/baseline_v1/`。
- `runs/my_baseline_v1/` 的本地留出指标、测试预测标签与上述运行一致。
- 当前模型：分设备 LightGBM，仅使用当前时刻原始 `feature_*`。
- 默认参数：300 轮、学习率 0.05、15 个叶子、叶节点最少 100 点、L2=5、seed=42、4 线程。
- 默认时间划分：4 月、7 月开始的两个校准块，9 月开始的最终留出块；训练与验证之间排除至少 72 点，并保护异常事件边界。
- 本地评分实现：`50 × Accuracy + 50 × 异常加权召回率`。

本地评分来自对网页公式的复现，不是官方评分脚本。官方页面在本轮分析中未能重新读取；跨设备汇总口径须以官方脚本或明确规则为准。线上分数与本地分数对应不同评测数据，不直接比较其差值。

### 1.2 已观测的留出结果

总分 **67.0528407011**；准确率 **87.4251%**；异常加权召回率 **46.6806%**。

| 设备 | 异常点数 | 漏报点数 | 误报点数 | 异常加权召回率 | 首次报警延迟 |
|---|---:|---:|---:|---:|---|
| P202A | 316 | 316 | 0 | 0% | 整段未检出 |
| P601B | 74 | 54 | 1,090 | 30.50% | 5 小时 |
| P310A | 140 | 63 | 27 | 54.34% | 4 小时 |
| P310B | 279 | 0 | 297 | 100% | 0 |
| P106A | 0 | 0 | 0 | 不可计算 | 无异常可评估 |
| P412B | 0 | 0 | 0 | 不可计算 | 无异常可评估 |

按当前本地整体公式，P202A 对满分差额的贡献为约 20.61 分，P601B 约 7.07 分。这是错误归因，不是可实现的提分承诺。

补充诊断：

- P202A 留出模型只见过 25 个训练异常点，留出 316 个异常点全部漏检。
- P601B 留出异常概率中位数约 0.00809，正常概率中位数约 0.00864；仅移动阈值难以解决分数重叠。
- P310B 的 297 个误报点中，290 个位于持续至少 18 点（6 小时）的误报段，孤立点过滤覆盖不了主要错误。
- P202A、P310A、P412B 的有效校准异常事件数均为 0，当前共用跨设备阈值约 0.000790566。
- 全部训练异常事件数：P106A 5 段、P202A 2 段、P310A 2 段、P310B 7 段、P412B 1 段、P601B 10 段。连续异常段不等于已确认的独立故障类型。

## 2. 所有实验共用的约束

### 2.1 独立实验的含义

每个实验从同一个 baselinev1 出发，只改变该实验声明的因素；不得默认叠加上一项实验的改动。需要前置实现的实验必须记录其父实验。最终组合实验另行编号。

固定原始数据、设备范围、随机种子和时间划分。模型训练类实验先用 seed=42 做筛选；仅对候选方案补充少量种子验证，避免把偶然波动当作收益。

### 2.2 验证与调参边界

1. 保留原始 baselinev1 的模型、概率、配置和提交结果，禁止覆盖。
2. 每项实验先在历史开发时间块选择参数或阈值；参数选定后才查看后续评估块。
3. 现有 fold2 已被用于定位问题。若继续依据它选模型，应称为开发诊断成绩，不能继续宣称是完全独立测试。
4. 在可用历史内设计事件完整的滚动向前评估：每次用过去训练、过去校准、随后时间块评估；明确每折异常事件覆盖及单类训练情况。
5. 新划分仍来自已经看过的数据，不能宣称获得了新的、完全未见的测试集。最终泛化证据需要未参与开发的数据或后续官方评测。
6. 单设备没有足够事件时如实标注证据不足。不得通过随机拆分同一异常事件制造训练和验证样本。
7. 所有拟合统计量、特征选择、PCA、缩放等仅在相应训练折拟合；不得用全量训练模型的重要性筛选历史验证特征。
8. 不根据测试预测异常比例手工指定标签；不把 A 榜多次提交当作无成本调参集。

### 2.3 共用评价输出

| 层级 | 必须记录的指标 |
|---|---|
| 整体 | 当前公式总分、Accuracy、异常加权召回率，以及相对固定基线的变化 |
| 设备 | FP、FN、误报率、普通召回、加权召回、阈值及其来源 |
| 事件 | 起止时间、长度、首次命中延迟、前 3/6 小时召回、整段加权召回 |
| 误报段 | 段数、持续时间分布、长时间误报点数 |
| 资源 | 训练耗时、推理耗时、峰值内存、特征数量 |

整段未检出的事件，首次命中延迟记录为缺失并标记 `missed_event=true`，不能用 0 表示。事件短于评价窗口时，以实际事件长度计算前段召回。无异常设备块只报告误报等可计算指标，不能把异常召回记为满分。

候选方案需在多个可用时间块/事件上表现稳定；如果整体收益仅来自一段长异常，应明确记录。单次本地上涨不足以确认线上提升。

## 3. 实验总览

| 编号 | 独立变量 | 主要文件 | 重点设备 | 建议顺序 |
|---|---|---|---|---|
| EXP-01 | 阈值搜索使用统一整体分母 | `metrics.py`、`train.py` | 全部 | 第一批 |
| EXP-02 | 设备概率尺度与阈值迁移策略 | `train.py`、`metrics.py`、`predict.py` | P202A/P310A/P412B | 第一批 |
| EXP-03 | 少量因果时序特征 | `baseline.py`、`train.py`、`predict.py` | P601B/P310A/P202A | 第二批 |
| EXP-04 | 训练样本权重 | `baseline.py`、`train.py` | P310A/P601B | 第二批 |
| EXP-05 | 单项 LightGBM 参数 | `baseline.py`、`train.py` | P202A 及全部对照 | 第三批 |
| EXP-06 | 正常状态异常检测分支 | `baseline.py`、`train.py`、`predict.py` | P202A/P310A/P412B | 第三批 |
| EXP-07 | 因果报警后处理 | 新增共享后处理模块、`train.py`、`predict.py` | P310B/P601B | 第四批 |
| EXP-08 | 已验证改动的组合及移除消融 | 上述相关文件 | 全部 | 最后 |

## 4. EXP-01：整体目标一致的阈值搜索

**假设**：当前逐设备优化单设备分数，可能与最终整体分数不一致。统一分母后，误报与漏报的权衡更接近当前整体评价目标。

**代码位置**：`metrics.py:52` 的 `choose_threshold()`，`train.py:63` 的校准汇总及 `train.py:75` 的设备阈值选择。

当前单设备搜索使用设备自己的 N 和 W；`aggregate()` 使用跨设备总 N 和总 W。若官方确认采用此整体公式，设备 d 内将一个预测从 0 翻为 1 的增益应为：

\[
\Delta S_i=50(2y_i-1)/N_{global}+50w_i/W_{global}.
\]

**改动**：允许 `choose_threshold()` 接收共享分母，在相同校准块和相同概率上比较原策略与统一分母策略。实验固定有效监督校准块的纳入规则；这里的 global 指本次共同纳入的全部校准块，不引入新数据。

**控制变量**：模型、原始概率、阈值候选扫描、设备阈值资格条件和缺少事件设备的回退策略均保持原样。

**验证**：用小型人工例子暴力枚举阈值，与新扫描结果核对；在相同校准集合检查新目标；再报告后续块结果。校准目标变好属于预期行为，后续块变好才支持迁移收益。

**注意**：如果官方按设备分数平均或采用其他汇总方式，应先按实际公式改写目标。不得把未确认口径的调整称为已修复官方评分错误。

## 5. EXP-02：跨设备阈值迁移

**假设**：独立训练的设备模型概率尺度不同，直接共用一个概率阈值不稳定。

**代码位置**：`train.py:63–76`；`metrics.py`；推理阈值应用位于 `predict.py:40`。

拆成两个子实验，分别与原基线比较：

- **EXP-02A：正常分数百分位校准。** 用历史折外正常样本概率拟合各设备经验分布，将概率转换为相对正常分布的百分位，再在有有效校准异常的开发块选择共享触发水平。映射拟合块与参数选择/评估块按时间分开，记录尾部样本数量。
- **EXP-02B：设备阈值收缩。** 对有设备校准证据的设备，把设备阈值向共享阈值收缩；可在 logit 空间使用收缩系数 0、0.5、1，计算前裁剪 0/1 概率。只比较此收缩因素，不叠加 EXP-02A。

**控制变量**：不重训模型，不改变特征、报警后处理和评分公式。

**重点检查**：误报率是否更稳定，异常召回是否受损；历史模型与全量重训模型之间的分数尺度是否仍有变化。

**边界**：P310A/P412B 当前前两折没有有效监督概率，不能直接构建可靠设备映射。需要先补充合法的历史预测块；仍不具备条件时保留回退并标注。正常样本可以约束误报，不能证明对新故障有召回能力。EXP-02B 本身不能解决完全没有设备校准事件的问题。

## 6. EXP-03：因果时序特征

**假设**：相对历史偏离和变化速度比单点绝对值更有助于区分异常与正常工况变化。

**代码位置**：新增共享 `build_features()`；替换 `train.py:44`、`train.py:91` 的原始输入构造；修改 `predict.py:31–37` 的推理输入和历史上下文读取。

**共同设置**：保留全部原始特征，在训练折内部选取 gain 排名前 30 的特征生成派生特征；没有有效监督模型时采用预先确定且只依赖训练数据的回退策略，并记录该折。最终全量重训时重新拟合筛选，保存最终特征名单。

| 子实验 | 新增特征 | 窗口/滞后 | 与谁比较 |
|---|---|---|---|
| EXP-03A | 当前值减过去均值 | 3、18、72 点 | 原基线 |
| EXP-03B | 当前值减历史点值 | 1、3、18 点 | 原基线 |
| EXP-03C | 历史标准差 | 3、18、72 点 | 原基线 |
| EXP-03D | 当前值相对历史均值的标准化偏离 | 18、72 点 | 原基线 |

历史参照使用 `shift(1).rolling(...)`。标准化偏离的分母保护值须明确且基于训练尺度；避免近零方差造成数值爆炸。先分别比较特征族，胜出后再做组合。

**控制变量**：固定 LightGBM 参数、训练权重、原阈值选择算法。每个新模型按同样算法重新校准阈值，不能直接复用旧概率阈值。

**实现要求**：

- 不使用居中窗口、未来回填或未来标签。
- 验证时允许读取此前已观测的特征作为历史上下文，包括隔离区间的特征；隔离区间标签仍不参与拟合。
- 推理接上训练末尾至少 72 点的原始特征，只输出测试行；明确时间不连续时的处理。
- 开头历史不足可保留 NaN 交给模型，但需要显式区分合法派生 NaN 与非法原始缺失，禁止未来值填充。
- 在 manifest 保存特征构造版本、窗口、筛选名单和输出列顺序；验证与推理使用同一实现。

**必要验证**：修改未来输入不影响过去特征；批量与分块构造在完整历史条件下输出一致；训练/测试接缝窗口正确。

**成功证据**：P601B 的分数区分改善、P310A 起始延迟缩短，且其他时间块没有明显恶化。不能只看训练分数或特征重要性。

## 7. EXP-04：训练样本权重

**假设**：在训练中强调异常及其前端，可改善起始漏检。

**代码位置**：`baseline.py:54` 的 `fit_model()` 增加 `sample_weight`，传入 LightGBM Dataset；`train.py:48` 和 `train.py:91` 同步提供训练折/全量训练权重。

独立比较：

| 子实验 | 正常样本权重 | 异常样本权重 | 用途 |
|---|---:|---|---|
| EXP-04A | 1 | 固定 2 或 4 | 判断普通类别加权是否有效 |
| EXP-04B | 1 | `c × event_weight / 4`，c 与 A 相同 | 比较相同平均权重下强调事件前端的收益 |

长于 1 点的事件权重平均为 4；单点事件需单独明确归一化策略，避免无意改变实验含义。事件权重只从当前训练标签计算。

**控制变量**：原始特征、固定树参数；沿用同一种阈值选择算法并重新校准。A 与 B 使用相同 c 时，才便于分离“整体异常权重”与“事件前端权重”的作用。

**成功证据**：前 3/6 小时召回和首次报警延迟改善，同时整体评分没有被新增误报抵消。不要用训练加权 logloss 代替最终评价。

## 8. EXP-05：模型容量与训练轮数

**假设**：统一的小树和叶节点约束可能限制部分设备，但放宽后也可能记住单个事件。

**代码位置**：`baseline.py:58–62`；`train.py` 参数解析及配置保存。

分别进行单因素实验：

| 子实验 | 改变的参数 | 候选 | 其余保持 |
|---|---|---|---|
| EXP-05A | min_data_in_leaf | 20、50、100 | 基线值 |
| EXP-05B | num_leaves | 7、15、31 | 基线值 |
| EXP-05C | num_boost_round | 100、300、600 | 基线值 |

不把三个候选集合直接做全网格。先分析 P202A，再检查其他设备是否有一致收益。若使用 early stopping，只能使用训练内部的开发块，并在后续评估前固定轮数。

**成功证据**：多个时间块改善，而不是只在已见事件附近预测更好。记录最优配置是否对设备高度敏感。

## 9. EXP-06：正常状态模型

**假设**：对少故障设备，建模正常状态可能补充监督模型对未见故障的识别。

**代码位置**：`baseline.py` 增加正常模型拟合和评分接口；`train.py` 增加训练分支及报告；`predict.py` 加载对应预处理器和模型。

先做一个受控方案：训练折正常样本上的稳健缩放 + PCA 重构误差。缩放尺度、PCA、维数选择全部限于训练折，不接触验证或测试拟合统计量。

- **EXP-06A：单独正常模型。** 在合法历史校准块选择阈值；没有异常校准样本时，可按预定正常尾部分位数设置候选，但必须报告不能验证召回。
- **EXP-06B：融合。** 只有 A 完成后才执行。先将监督分数和重构误差用历史校准映射到可比较尺度，再试少量固定融合权重。A 和原基线都是对照。

**控制变量**：使用当前时刻原始特征，不叠加时序特征或复杂后处理。

**成功证据**：未见事件召回提升，正常工况变化没有造成大面积误报。P412B 只有一个异常事件，无法同时提供独立故障监督训练和独立故障验证，应单独标注证据边界。

## 10. EXP-07：报警后处理

**假设**：对已有有效信号进行轻量时间连续性处理，可降低碎片化或短暂掉报。

**代码位置**：新增共享 `postprocess_probabilities()`，在 `train.py:79` 的验证预测和 `predict.py:40` 的推理中共同调用。

独立比较：

- **EXP-07A：滞回阈值。** 达到较高阈值立即触发，触发后用较低阈值维持；不等待连续多个高分点才开始报警。
- **EXP-07B：短因果平滑。** 仅使用当前和过去概率，比较 2/3 点窗口；不叠加 A。

高低阈值或平滑后阈值只在开发校准数据选择。说明验证块开始时的状态初始化，避免把前一折不同模型的状态直接带入。

**控制变量**：固定基线模型及原始概率，不重新训练。

**成功证据**：整体分数、起始延迟及长误报段同时检查。P310B 当前错误以长误报段为主，因此不把孤立点过滤列为主要提分方向。禁止利用真实标签事后填满异常段，或使用未来概率回填过去预测。

## 11. EXP-08：有效改动组合

只组合已经完成独立验证的改动。例如，时序特征与训练权重均有效后，比较：

1. 原基线。
2. 仅时序特征。
3. 仅训练权重。
4. 时序特征 + 训练权重。

然后再加入通过验证的阈值策略，并对最终组合逐项移除。所有组合都重新进行训练和阈值校准，不能把单项收益相加作为组合收益。

完成六设备推理一致性、结果行数和时间戳、模型与 manifest、打包依赖检查后，才生成独立候选提交包。新增模块需同步加入 `package_submission.py` 的源码清单。

## 12. 实验目录与记录模板

建议独立目录：

```text
runs/exp01_global_threshold/
runs/exp02a_normal_percentile/
runs/exp02b_threshold_shrinkage/
runs/exp03a_history_deviation/
runs/exp03b_lag_difference/
runs/exp03c_history_std/
runs/exp03d_standardized_deviation/
runs/exp04a_class_weight/
runs/exp04b_event_weight/
runs/exp05a_leaf_samples/
runs/exp05b_num_leaves/
runs/exp05c_rounds/
runs/exp06a_normal_pca/
runs/exp06b_score_fusion/
runs/exp07a_hysteresis/
runs/exp07b_causal_smoothing/
runs/exp08_combination/
```

每个候选配置再使用子目录，遵循现有脚本拒绝覆盖非空输出目录的规则。至少保存配置、代码版本/差异、数据校验值、验证概率、逐设备及逐事件指标；训练实验还需保存模型、预处理状态和特征清单。

每项实验使用以下模板记录结果：

```markdown
# EXP-XX 实验结果

- 状态：待实施 / 已运行 / 保留 / 放弃 / 证据不足
- 对照运行：
- 父实验：无，或明确编号
- 代码版本与改动：
- 唯一改变的因素：
- 数据与时间划分：
- 参数/阈值选择使用的数据：
- 评估数据是否已被用于开发：
- 实际执行命令：
- 输出目录：

| 指标 | 固定基线 | 本实验 | 差值 |
|---|---:|---:|---:|
| 整体分数 | | | |
| Accuracy | | | |
| 异常加权召回 | | | |
| 训练秒数 | | | |
| 推理秒数 | | | |
| 峰值内存 | | | |

逐设备和逐事件结果：附文件路径。

主要收益来自哪些事件：
哪些设备/事件退化：
收益是否跨时间块稳定：
上线 A 榜结果：未提交，或真实记录。
结论及下一步：
```

## 13. 第一轮执行范围

先实现共用诊断输出和 EXP-01；再开展 EXP-03A 与 EXP-04A/04B。EXP-02 先审查可用校准证据，不强行为缺少有效历史概率的设备生成校准器。EXP-05、EXP-06、EXP-07 在前述结果明确后依次推进。

保留 CPU 实验流程。现有基线报告全流程约 81.3 秒、峰值 RSS 约 598 MiB，这是该次运行实测，不代表新增实验开销。新增方案逐项实测后决定是否需要更大资源。
